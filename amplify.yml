version: 1
frontend:
  phases:
    preBuild:
      commands:
        - chmod 777 patch-next.sh
        - npm install
        - ./patch-next.sh
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
